import 'package:flutter/material.dart';

class AmulateDevice extends StatelessWidget {
  const AmulateDevice({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}